import React from "react";
import ComponentPanel from '../../components/componentsPanel'
// import { MENU_CONFIG } from "../../../constants";

const Menu = () => (
  <div>
    <h1>主菜单</h1>
    <h2>组件面板</h2>
    <ComponentPanel/>
  </div>
);

export default Menu;
